# Give Node-RED full access to everything
user nodered
topic readwrite #

# Gateway can publish to its own subtree (iot/{site}/{gateway_id}/...)
pattern write iot/+/%u/#

# Gateway can subscribe to commands addressed to it
pattern read iot/+/%u/#

